{
  "name": "kensai-music-db-1",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "6.9.0",
    "npm": "4.1.1"
  },
  "scripts": {
    "postinstall": "cd client && yarn install && yarn run build:development",
    "rails-server": "foreman start -f Procfile.dev",
    "test": "rspec"
  }
}
